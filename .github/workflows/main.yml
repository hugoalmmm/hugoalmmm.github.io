name: Deploying project on GH Pages

on:
  push:
     branches: ["main"]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: testing ls
        run: ls -al
      - name : Checkout
        uses : actions/ccheckout@v4
      - name: testing ls
        run: ls -al
        